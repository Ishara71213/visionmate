import 'package:flutter/material.dart';

class GuideDescriptionCommon extends StatelessWidget {
  const GuideDescriptionCommon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
