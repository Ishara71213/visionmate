import 'package:flutter/material.dart';

class VoiceAssistanceGuideDescription extends StatelessWidget {
  const VoiceAssistanceGuideDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
